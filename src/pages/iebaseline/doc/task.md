# User Assignment Status Module
 - Display all group by User Name(user id as unique identifier), showing all assignemnt status. Only from user_checklist_status. Meaning completed assignment shouldnt show here as Admin has approved it.
 - The purpose of this module is to allow admin to keep track the completion status of the user and perhaps sent out reminders(notification feature maybe in the v2 version xD)
 - It would be nice to include a search(user name, module name) or filter by Status feature.
 - The list show based on the "user_checklist_status" table. 
 - Drop backend related changes in src\pages\iebaseline\doc\backend-handoff.md
 - New route means new RBAC seed.
